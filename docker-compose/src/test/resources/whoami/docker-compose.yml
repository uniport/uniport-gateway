version: "3"

services:
    whoami:
        image: ${docker.pull.registry}/traefik/whoami
        expose:
            - "80"
        networks:
            - portal-gateway
        labels:
            - "portal.enable=true"
            - "portal.http.routers.root.rule=Path('/')"

networks:
    portal-gateway:
        external: true
