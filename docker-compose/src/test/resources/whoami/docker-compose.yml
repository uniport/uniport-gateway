version: "3"

services:
  whoami:
    image: traefik/whoami
    ports:
      - "8080:80"
      - "8081:80"
    labels:
      - "portal.http.routers.root.rule=Path('/')"
      - "portal.http.services.whoami-service.servers.port=8080"
