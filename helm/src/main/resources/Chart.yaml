apiVersion: v2
name: uniport-gateway
description: A Helm chart for configuring uniport-gateway on Kubernetes

type: application

version: ${project.version}
appVersion: ${project.version}

dependencies:
  - name: portal-lib
    version: "${portal-helm-chart-library.version}"
    repository: "${portal-helm-dependencies.repository}"
